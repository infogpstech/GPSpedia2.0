GPSpedia - REGISTRO DE CAMBIOS VERIFICADO (POR ARCHIVO)
=================================================
Este changelog sigue un versionamiento por archivo y solo incluye cambios verificados.

-------------------------------------------------
**VERSIONES DE COMPONENTES ACTIVOS**
-------------------------------------------------
- `index.html`: 4.3.6
- `add_cortes.html`: 2.1.8
- `api-manager.js`: 3.9.2
- `services/auth/auth.js`: 2.2.1
- `services/catalog/catalog.js`: 2.3.0
- `services/write/write.js`: 2.3.0
- `services/feedback/feedback.js`: 2.2.0
- `services/users/users.js`: 2.2.1
- `services/utilities/utilities.js`: 1.0.0
-------------------------------------------------

**HISTORIAL DE CAMBIOS VERIFICADOS**

*   **File:** `api-manager.js`
    *   **v3.9.1 -> v3.9.2:** Se agregaron las acciones `addOrUpdateCut` y `addSupplementaryInfo` al mapa de enrutamiento (`ACTION_TO_SERVICE_MAP`), corrigiendo un error de "Acción no definida".

*   **File:** `add_cortes.html`
    *   **v2.1.7 -> v2.1.8:** Se refactorizó el formulario (Etapa 2) para utilizar un sistema de subida de archivos (`input[type="file"]`) con vista previa, en lugar del campo de URL anterior.
    *   **v2.1.6 -> v2.1.7:** Se añadió un bloque `try...catch` a la función `handleSuggestion` para prevenir que un fallo en la API detenga la ejecución del script.
    *   **v2.1.5 -> v2.1.6:** Se solucionó un bug donde la galería de imágenes (`SimpleLightbox`) no se inicializaba para los resultados de búsqueda cargados dinámicamente.
    *   **v2.1.4 -> v2.1.5:** Se mejoró el flujo de anti-duplicados reemplazando el modal de elección por botones de acción inline.
    *   **v2.1.3 -> v2.1.4:** Se modificó el flujo de anti-duplicados para presentar un modal con opciones claras.
    *   **v2.1.2 -> v2.1.3:** Se mejoró la usabilidad del formulario añadiendo un botón explícito de "Seleccionar este Vehículo" y gestionando el historial del navegador.
    *   **v2.1.1 -> v2.1.2:** Se integró una galería de imágenes (`SimpleLightbox`) en los resultados de búsqueda.
    *   **v2.1.0 -> v2.1.1:** Se rediseñó la visualización de resultados en la búsqueda de vehículos para mostrar tarjetas detalladas.

*   **File:** `services/write/write.js`
    *   **v2.2.1 -> v2.3.0:** Se refactorizó la lógica de subida de imágenes para crear una estructura de carpetas jerárquica en Google Drive y renombrar los archivos a un formato estandarizado.

*   **File:** `services/catalog/catalog.js`
    *   **v2.2.0 -> v2.3.0:** Se implementó la lógica de búsqueda flexible en `handleCheckVehicle` (búsqueda parcial e insensible a mayúsculas/minúsculas).
    *   **v2.1.0 -> v2.2.0:** Se implementó la funcionalidad "Quizás quisiste decir..." utilizando el algoritmo de distancia de Levenshtein.
    *   **v2.0.0 -> v2.1.0:** Se solucionó un `TypeError` crítico en `handleGetDropdownData` corrigiendo la estructura del JSON devuelto.
    *   **v1.2.1 -> v2.0.0:** Se corrigió un `TypeError: ... .getValues is not a function`.

*   **File:** `index.html`
    *   **v4.3.5 -> v4.3.6:** Se resolvió un `ReferenceError: crearCarrusel is not defined` moviendo la función al ámbito global.
    *   **v4.3.4 -> v4.3.5:** Se generalizó la lógica de navegación de los carruseles en una función `crearCarrusel` reutilizable.
    *   **v4.3.3 -> v4.3.4:** Se rediseñó el modal de detalles del vehículo para alinearlo con la especificación de 12 puntos del `README.md`.
    *   **v4.3.2 -> v4.3.3:** Se corrigió la función `handleLike` para asegurar que el `userId` se envíe correctamente al backend.
    *   **v4.3.1 -> v4.3.2:** Se mejoró la función `getLogoUrlForMarca` con un algoritmo de búsqueda flexible.
    *   **v4.3.0 -> v4.3.1:** Se resolvió un `ReferenceError: updateCarousel is not defined`.
    *   **v4.2.0 -> v4.3.0:** Se implementó la lógica de frontend para la sugerencia de año.
    *   **v4.1.1 -> v4.2.0:** Se restauró la lógica para el botón de instalación de la PWA.
    *   **v4.1.0 -> v4.1.1:** Se integró el contenido de `info.html` como modales.
    *   **v4.0.0 -> v4.1.0:** Se implementaron las interfaces para "Inbox" y "Herramientas de Desarrollador".

*   **File:** `services/feedback/feedback.js`
    *   **v2.1.0 -> v2.2.0:** Se añadieron los endpoints para la funcionalidad de "Inbox" (`getFeedbackItems`, `replyToFeedback`, `markAsResolved`).

*   **File:** `services/users/users.js`
    *   **v2.2.0 -> v2.2.1:** Se solucionó un bug que causaba que los datos de los usuarios se mostraran como "undefined" al corregir el acceso a propiedades.

*   **File:** `services/utilities/utilities.js`
    *   **v1.0.0:** Versión inicial del servicio.
